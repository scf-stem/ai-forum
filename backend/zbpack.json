{
  "python": {
    "package_manager": "pip",
    "entry": "app/main.py",
    "framework": "fastapi"
  },
  "build_command": "alembic upgrade head",
  "start_command": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
}
